<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.bazaar.mapper.CouponMapper">

    <select id="queryCouponOfGoods" resultType="com.campus.bazaar.entity.Coupon" parameterType="java.lang.Long">
        SELECT
            v.`id`, v.`goods_id`, v.`title`, v.`sub_title`, v.`rules`, v.`pay_value`,
            v.`actual_value`, v.`type`, sv.`stock` , sv.begin_time , sv.end_time
        FROM tb_coupon v
        LEFT JOIN  tb_seckill_coupon sv ON v.id = sv.coupon_id
        WHERE v.goods_id = #{goodsId} AND v.status = 1
    </select>
</mapper>
